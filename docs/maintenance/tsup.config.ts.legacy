import { defineConfig } from 'tsup';

export default defineConfig({
  entry: {
    'index': 'praxis/index.ts',
    'plugin': 'praxis/plugin.ts'
  },
  format: ['cjs', 'esm'],
  dts: true,
  clean: true,
  splitting: false,
  sourcemap: true,
  external: ['vite'],
  target: 'node16'
});
